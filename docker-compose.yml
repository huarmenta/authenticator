version: '3'

services:
  db:
    image: postgres:10.5-alpine
    volumes:
      - db-data:/var/lib/postgresql/data
    environment:
      - POSTGRES_USER=condovive
      - POSTGRES_DB=authenticator
  app:
    build:
      context: .
      args:
        - BUNDLE_WITHOUT=
        - RAILS_ENV=development
    volumes:
      - .:/app
    depends_on:
      - db
    env_file: .env
    environment:
      - DB_HOST=db

volumes:
  db-data:
